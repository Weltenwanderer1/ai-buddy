# AI-Buddy Phase 1: Core Fixes — Implementation Plan

**Goal:** Fix critical architecture gaps, add robustness, prepare foundation for voice features.

**Models:**
- Primary: `glm-5.1` via Ollama Cloud
- Fallback: `minimax-m2.7:cloud` via Ollama Cloud

**Architektur-Kurzfassung:**
- `OllamaCloudService` wird um Timeout, Retry, Fallback-Model, Safe-JSON erweitert.
- `ChatService` orchestriert System-Prompt + Memory-Kontext + History → LLM → Memory speichern.
- `MemoryService` bekommt `getRelevantMemories(query, limit)`.
- `ChatHistoryService` speichert Messages in Datei → App-Restart überlebt.
- Alle JSON-Dateien bekommen try-catch.
- Settings kriegen vernünftige Defaults + Debounce-Save.
- `pubspec.yaml` bekommt fehlende Packages.

---

## Task 1: `pubspec.yaml` — Fehlende Dependencies ergänzen

**Files:**
- Modify: `/home/gsus/GoogleDrive/_Programmieren/AI-Buddy/pubspec.yaml`

```yaml
dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  path_provider: ^2.1.0
  shared_preferences: ^2.2.0
  provider: ^6.1.0
  flutter_dotenv: ^5.1.0
  archive: ^3.4.0
  uuid: ^4.2.0
  intl: ^0.19.0
  file_picker: ^6.1.0
  flutter_secure_storage: ^9.2.0    # NEU: API Keys sicher speichern
  speech_to_text: ^6.6.0            # NEU: Spracherkennung
  just_audio: ^0.9.40                # NEU: Audio playback
  rxdart: ^0.28.0                   # NEU: Debouncing, Streams
```

**Schritt:** Datei patchen, dann `flutter pub get` im AI-Buddy-Verzeichnis ausführen.

---

## Task 2: `.env.example` — Fallback Model + Missing Keys

**Files:**
- Modify: `/home/gsus/GoogleDrive/_Programmieren/AI-Buddy/.env.example`

```env
# Ollama Cloud
OLLAMA_CLOUD_BASE_URL=https://api.ohmyllama.com
OLLAMA_CLOUD_API_KEY=your-ollama-api-key-here
OLLAMA_CLOUD_MODEL=glm-5.1
OLLAMA_CLOUD_FALLBACK_MODEL=minimax-m2.7:cloud

# ElevenLabs
ELEVENLABS_API_KEY=your-elevenlabs-api-key-here
ELEVENLABS_VOICE_ID=your-voice-id-here
ELEVENLABS_MODEL_ID=eleven_multilingual_v2
```

---

## Task 3: `lib/services/ollama_cloud_service.dart` — Robuste Version

**Files:**
- Modify: `/home/gsus/GoogleDrive/_Programmieren/AI-Buddy/lib/services/ollama_cloud_service.dart`

```dart
import 'dart:convert';
import 'package:http/http.dart' as http;
import 'package:flutter_dotenv/flutter_dotenv.dart';

/// Service for communicating with Ollama Cloud LLM API.
/// Supports primary + fallback model, timeout, retry, safe JSON parsing.
class OllamaCloudService {
  String get baseUrl =>
      dotenv.env['OLLAMA_CLOUD_BASE_URL'] ?? 'https://api.ohmyllama.com';
  String get apiKey =>
      dotenv.env['OLLAMA_CLOUD_API_KEY'] ?? '';
  String get defaultModel =>
      dotenv.env['OLLAMA_CLOUD_MODEL'] ?? 'glm-5.1';
  String get fallbackModel =>
      dotenv.env['OLLAMA_CLOUD_FALLBACK_MODEL'] ?? 'minimax-m2.7:cloud';

  static const _timeout = Duration(seconds: 120);
  static const _maxRetries = 2;

  Future<String> chat({
    required String systemPrompt,
    required List<Map<String, String>> messages,
    String? model,
    double temperature = 0.7,
  }) async {
    final allMessages = [
      {'role': 'system', 'content': systemPrompt},
      ...messages,
    ];

    // Try primary model with retries
    String? lastError;
    for (int attempt = 0; attempt <= _maxRetries; attempt++) {
      try {
        return await _doRequest(
          allMessages,
          model ?? defaultModel,
          temperature,
        );
      } on TimeoutException {
        lastError = 'Timeout after $_timeout';
      } on http.ClientException catch (e) {
        lastError = 'Network error: $e';
      } catch (e) {
        final status = _extractStatus(e);
        if (status == 429 || status >= 500) {
          lastError = 'Retryable error ($status): $e';
          await Future.delayed(Duration(seconds: 2 * (attempt + 1)));
          continue;
        }
        rethrow;
      }
    }

    // Fallback to secondary model
    if (model == null) {
      try {
        return await _doRequest(
          allMessages,
          fallbackModel,
          temperature,
        );
      } catch (e) {
        throw Exception('Primary model failed: $lastError; Fallback failed: $e');
      }
    }
    throw Exception('All retries exhausted: $lastError');
  }

  Future<String> _doRequest(
    List<Map<String, String>> messages,
    String model,
    double temperature,
  ) async {
    final client = http.Client();
    try {
      final response = await client
          .post(
            Uri.parse('$baseUrl/v1/chat/completions'),
            headers: {
              'Content-Type': 'application/json',
              if (apiKey.isNotEmpty) 'Authorization': 'Bearer $apiKey',
            },
            body: jsonEncode({
              'model': model,
              'messages': messages,
              'temperature': temperature,
            }),
          )
          .timeout(_timeout);

      if (response.statusCode == 200) {
        final data = jsonDecode(response.body);
        final content = data['choices']?[0]?['message']?['content'];
        if (content == null) {
          throw FormatException('Unexpected API response shape: ${response.body}');
        }
        return content as String;
      } else {
        throw Exception('${response.statusCode}: ${response.body}');
      }
    } finally {
      client.close();
    }
  }

  int? _extractStatus(dynamic e) {
    final msg = e.toString();
    final match = RegExp(r'\b([45]\d{2})\b').firstMatch(msg);
    return match != null ? int.tryParse(match.group(1)!) : null;
  }
}

class TimeoutException implements Exception {
  final String message;
  TimeoutException(this.message);
  @override
  String toString() => message;
}
```

---

## Task 4: `lib/models/chat_message.dart` — Datenmodell erweitern

**Files:**
- Modify: `/home/gsus/GoogleDrive/_Programmieren/AI-Buddy/lib/models/chat_message.dart`

```dart
import 'package:uuid/uuid.dart';

enum MessageType { text, system, error, voice }

class ChatMessage {
  final String id;
  final String text;
  final bool isUser;
  final DateTime timestamp;
  final MessageType type;

  ChatMessage({
    String? id,
    required this.text,
    required this.isUser,
    DateTime? timestamp,
    this.type = MessageType.text,
  })  : id = id ?? const Uuid().v4(),
        timestamp = timestamp ?? DateTime.now();

  Map<String, dynamic> toJson() => {
        'id': id,
        'text': text,
        'isUser': isUser,
        'timestamp': timestamp.toIso8601String(),
        'type': type.name,
      };

  factory ChatMessage.fromJson(Map<String, dynamic> json) => ChatMessage(
        id: json['id'] as String? ?? const Uuid().v4(),
        text: json['text'] as String? ?? '',
        isUser: json['isUser'] as bool? ?? false,
        timestamp: json['timestamp'] != null
            ? DateTime.tryParse(json['timestamp'] as String) ?? DateTime.now()
            : DateTime.now(),
        type: MessageType.values.firstWhere(
          (t) => t.name == (json['type'] as String?),
          orElse: () => MessageType.text,
        ),
      );

  @override
  bool operator ==(Object other) =>
      identical(this, other) || (other is ChatMessage && other.id == id);

  @override
  int get hashCode => id.hashCode;
}
```

---

## Task 5: `lib/services/chat_history_service.dart` — Chat-Persistenz

**Files:**
- Create: `/home/gsus/GoogleDrive/_Programmieren/AI-Buddy/lib/services/chat_history_service.dart`

```dart
import 'dart:convert';
import 'dart:io';
import 'package:flutter/material.dart';
import 'package:path_provider/path_provider.dart';
import '../models/chat_message.dart';

/// Persists chat messages to disk so they survive app restarts.
class ChatHistoryService extends ChangeNotifier {
  static const _maxMessages = 200;
  List<ChatMessage> _messages = [];
  late File _file;

  List<ChatMessage> get messages => List.unmodifiable(_messages);

  Future<void> init() async {
    final dir = await getApplicationDocumentsDirectory();
    _file = File('${dir.path}/ai_buddy/chat_history.json');
    await _load();
  }

  Future<void> add(ChatMessage message) async {
    _messages.add(message);
    if (_messages.length > _maxMessages) {
      _messages = _messages.sublist(_messages.length - _maxMessages);
    }
    notifyListeners();
    await _save();
  }

  Future<void> addAll(List<ChatMessage> messages) async {
    _messages.addAll(messages);
    if (_messages.length > _maxMessages) {
      _messages = _messages.sublist(_messages.length - _maxMessages);
    }
    notifyListeners();
    await _save();
  }

  Future<void> clear() async {
    _messages.clear();
    notifyListeners();
    await _save();
  }

  Future<void> _load() async {
    if (!await _file.exists()) return;
    try {
      final data = jsonDecode(await _file.readAsString()) as List;
      _messages = data
          .map((e) => ChatMessage.fromJson(e as Map<String, dynamic>))
          .toList();
    } catch (e) {
      // Corrupted file — start fresh
      _messages = [];
    }
    notifyListeners();
  }

  Future<void> _save() async {
    await _file.parent.create(recursive: true);
    await _file.writeAsString(
      jsonEncode(_messages.map((m) => m.toJson()).toList()),
    );
  }
}
```

---

## Task 6: `lib/services/memory_service.dart` — Safe JSON + Query-Methode

**Files:**
- Modify: `/home/gsus/GoogleDrive/_Programmieren/AI-Buddy/lib/services/memory_service.dart`

**Änderungen:**
1. `import 'package:uuid/uuid.dart';` → `const _uuid = Uuid();`
2. `_loadShortTerm()` wrap in `try-catch` → korrupte Datei = leer starten
3. `_loadLongTerm()` wrap in `try-catch` → korrupte Datei = leer starten
4. `addShortTerm`: `_uid()` → `_uuid.v4()`
5. `addLongTerm`: `_uid()` → `_uuid.v4()`
6. **Neue Methode** `getRelevantMemories(String query, {int limit = 5})` hinzufügen:

```dart
/// Returns memories relevant to a query, sorted by recency + similarity.
List<MemoryItem> getRelevantMemories(String query, {int limit = 5}) {
  if (query.trim().isEmpty) return [];

  final scored = <({MemoryItem item, double score})>[];
  for (final mem in [..._shortTerm, ..._longTerm]) {
    final sim = _similarity(query, mem.content);
    final ageHours = DateTime.now().difference(mem.timestamp).inHours;
    final recency = 1.0 / (1.0 + ageHours / 24.0); // newer = higher
    scored.add((item: mem, score: sim * 0.7 + recency * 0.3));
  }

  scored.sort((a, b) => b.score.compareTo(a.score));
  return scored.take(limit).map((s) => s.item).toList();
}
```

7. `_counter` + `_uid()` komplett entfernen (UUID nutzen)
8. Promotion-Delay entfernen (nach promotion aus shortTerm löschen reicht)

---

## Task 7: `lib/services/chat_service.dart` — Orchestration Engine

**Files:**
- Modify: `/home/gsus/GoogleDrive/_Programmieren/AI-Buddy/lib/services/chat_service.dart`

```dart
import '../services/ollama_cloud_service.dart';
import '../services/memory_service.dart';
import '../services/persona_service.dart';

/// Orchestrates LLM calls with system prompt, memory context, and history.
class ChatService {
  final OllamaCloudService _llm = OllamaCloudService();

  /// Sends a message, injects memory context, and stores in memory.
  Future<String> sendMessage({
    required String userMessage,
    required PersonaService persona,
    required MemoryService memory,
    required List<ChatMessage> history,
  }) async {
    // Build system prompt with memory
    final systemPrompt = _buildSystemPrompt(persona, memory);

    // Window history (last 20 messages to avoid context overflow)
    final windowedHistory = history.length > 20
        ? history.sublist(history.length - 20)
        : history;

    final historyMaps = windowedHistory.map((m) => {
          'role': m.isUser ? 'user' : 'assistant',
          'content': m.text,
        }).toList();

    // Call LLM
    final reply = await _llm.chat(
      systemPrompt: systemPrompt,
      messages: historyMaps,
    );

    // Store in memory
    await memory.addShortTerm(userMessage, source: 'user');
    await memory.addShortTerm(reply, source: 'assistant');

    return reply;
  }

  String _buildSystemPrompt(PersonaService persona, MemoryService memory) {
    final base = persona.buildSystemPrompt();

    final relevant = memory.getRelevantMemories(persona.name, limit: 5);
    if (relevant.isEmpty) return base;

    final memoryBlock = relevant
        .map((m) => '- [${m.source}/${m.timestamp.toIso8601String().split('T').first}]: ${m.content}')
        .join('\n');

    return '$base\n\nDein Gedächtnis (relevante Erinnerungen):\n$memoryBlock\nAntworte passend zu diesen Erinnerungen.';
  }
}
```

**Neuer Import in chat_service.dart:**
```dart
import '../models/chat_message.dart';
```

---

## Task 8: `lib/services/settings_service.dart` — Debounce + Safe JSON

**Files:**
- Modify: `/home/gsus/GoogleDrive/_Programmieren/AI-Buddy/lib/services/settings_service.dart`

```dart
import 'dart:convert';
import 'dart:io';
import 'package:path_provider/path_provider.dart';
import 'package:flutter/material.dart';
import 'package:rxdart/rxdart.dart';

/// Key-value settings with debounced save and safe JSON loading.
class SettingsService extends ChangeNotifier {
  static const _saveDebounceMs = 500;

  final Map<String, dynamic> _data = {};
  final _saveController = PublishSubject<void>();

  late File _file;

  dynamic operator [](String key) => _data[key];
  void operator []=(String key, dynamic value) {
    _data[key] = value;
    notifyListeners();
    _scheduleSave();
  }

  Future<void> init() async {
    final dir = await getApplicationDocumentsDirectory();
    _file = File('${dir.path}/ai_buddy/settings.json');
    _saveController
        .debounceTime(const Duration(milliseconds: _saveDebounceMs))
        .listen((_) => _doSave());
    await _load();
    _setDefaults();
    notifyListeners();
  }

  void _setDefaults() {
    _data.putIfAbsent('max_history', () => 20);
    _data.putIfAbsent('memory_promotion_threshold', () => 3);
    _data.putIfAbsent('memory_ttl_minutes', () => 60);
    _data.putIfAbsent('tts_enabled', () => false);
    _data.putIfAbsent('stt_enabled', () => false);
    _data.putIfAbsent('temperature', () => 0.7);
  }

  Future<void> _scheduleSave() async {
    _saveController.add(null);
  }

  Future<void> _doSave() async {
    await _file.parent.create(recursive: true);
    await _file.writeAsString(JsonEncoder.withIndent('  ').convert(_data));
  }

  Future<void> _load() async {
    if (!await _file.exists()) return;
    try {
      final raw = await _file.readAsString();
      final decoded = jsonDecode(raw) as Map<String, dynamic>;
      _data.addAll(decoded);
    } catch (e) {
      // Corrupted — keep defaults, don't crash
    }
  }

  Map<String, dynamic> exportData() => Map.from(_data);

  Future<void> importData(Map<String, dynamic> data) async {
    _data.clear();
    _data.addAll(data);
    notifyListeners();
    await _doSave();
  }

  @override
  void dispose() {
    _saveController.close();
    super.dispose();
  }
}
```

---

## Task 9: `lib/screens/chat_screen.dart` — ChatHistoryService integrieren

**Files:**
- Modify: `/home/gsus/GoogleDrive/_Programmieren/AI-Buddy/lib/screens/chat_screen.dart`

**Änderungen:**

1. `ChatHistoryService` importieren und `ChatService` injected bekommen
2. `ScrollController` hinzufügen für Auto-Scroll
3. In `_loadWelcome()`: gespeicherte Messages laden statt nur Welcome-Message
4. In `_sendMessage()`: MessageHistory persistieren
5. `ChatService` als `final` Property, injected
6. Error-Messages Sanitisieren (keine internen Details zeigen)

```dart
// Wichtigste Änderungen in _sendMessage:
final reply = await _chatService.sendMessage(
  userMessage: text,
  persona: persona,
  memory: memory,
  history: _chatHistory,
);
_chatHistory.add(ChatMessage(text: text, isUser: true));
_chatHistory.add(ChatMessage(text: reply, isUser: false));
```

---

## Task 10: `lib/main.dart` — ChatHistoryService einhängen

**Files:**
- Modify: `/home/gsus/GoogleDrive/_Programmieren/AI-Buddy/lib/main.dart`

```dart
// Nach memory.init() und vor runApp():
final chatHistory = ChatHistoryService();
await chatHistory.init();

// In MultiProvider hinzufügen:
ChangeNotifierProvider.value(value: chatHistory),

// AIBuddyApp bekommt neuen Parameter:
class AIBuddyApp extends StatelessWidget {
  final SettingsService settings;
  final MemoryService memory;
  final PersonaService persona;
  final ChatHistoryService chatHistory;
  ...
}

// home-Route bekommt ChatHistoryService injected:
home: persona.isComplete
    ? const ChatScreen()
    : OnboardingScreen(persona: persona),
```

---

## Task 11: `lib/services/elevenlabs_service.dart` — Echte Implementierung

**Files:**
- Modify: `/home/gsus/GoogleDrive/_Programmieren/AI-Buddy/lib/services/elevenlabs_service.dart`

```dart
import 'dart:convert';
import 'package:http/http.dart' as http;
import 'package:flutter_dotenv/flutter_dotenv.dart';

class ElevenLabsService {
  String get apiKey =>
      dotenv.env['ELEVENLABS_API_KEY'] ?? '';
  String get voiceId =>
      dotenv.env['ELEVENLABS_VOICE_ID'] ?? '';
  String get modelId =>
      dotenv.env['ELEVENLABS_MODEL_ID'] ?? 'eleven_multilingual_v2';

  bool get isAvailable => apiKey.isNotEmpty && voiceId.isNotEmpty;

  Future<List<int>> synthesize(String text) async {
    if (!isAvailable) return [];

    final response = await http.post(
      Uri.parse('https://api.elevenlabs.io/v1/text-to-speech/$voiceId'),
      headers: {
        'xi-api-key': apiKey,
        'Content-Type': 'application/json',
        'Accept': 'audio/mpeg',
      },
      body: jsonEncode({
        'text': text,
        'model_id': modelId,
        'voice_settings': {
          'stability': 0.5,
          'similarity_boost': 0.8,
        },
      }),
    );

    if (response.statusCode == 200) {
      return response.bodyBytes;
    } else {
      throw Exception('ElevenLabs error: ${response.statusCode}');
    }
  }
}
```

---

## Task 12: `android/app/src/main/AndroidManifest.xml` — Permissions

**Files:**
- Modify: `/home/gsus/GoogleDrive/_Programmieren/AI-Buddy/android/app/src/main/AndroidManifest.xml`

Innerhalb von `<manifest>`, nach `<application>`:

```xml
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.RECORD_AUDIO" />
```

---

## Task 13: README.md — Phase 1 Fortschritt dokumentieren

**Files:**
- Modify: `/home/gsus/GoogleDrive/_Programmieren/AI-Buddy/README.md`

CHANGELOG-Sektion hinzufügen:

```markdown
## Changelog

### Phase 1 (2026-04-26)
- [FIX] Memory-Kontext wird jetzt in ChatService injiziert
- [FIX] Chat-Verlauf persistent (überlebt App-Neustart)
- [FIX] OllamaCloudService: Timeout, Retry, Fallback-Model (minimax-m2.7:cloud)
- [FIX] Safe JSON parsing — korrupte Dateien crashen App nicht mehr
- [FIX] Settings: Debounced Save + Defaults
- [NEW] MemoryService.getRelevantMemories(query, limit)
- [NEW] ChatHistoryService
- [NEW] ElevenLabsService: Echte TTS-Implementierung
- [NEW] Primary model: glm-5.1, Fallback: minimax-m2.7:cloud
- [NEW] Android: INTERNET + RECORD_AUDIO Permissions
- [NEW] flutter_secure_storage, speech_to_text, just_audio, rxdart deps
```

---

## Execution Order

```
Task 1  → pubspec.yaml (Dependencies)
Task 2  → .env.example (Fallback Model)
Task 3  → ollama_cloud_service.dart (Timeout/Retry/Fallback)
Task 4  → chat_message.dart (UUID + MessageType)
Task 5  → chat_history_service.dart (NEU — Persistenz)
Task 6  → memory_service.dart (Safe JSON + getRelevantMemories)
Task 7  → chat_service.dart (Orchestration)
Task 8  → settings_service.dart (Debounce + Safe JSON)
Task 9  → chat_screen.dart (HistoryService + AutoScroll)
Task 10 → main.dart (ChatHistoryService einhängen)
Task 11 → elevenlabs_service.dart (Echte TTS)
Task 12 → AndroidManifest.xml (Permissions)
Task 13 → README.md (Dokumentation)
```

**Nach jeder Task:** `flutter analyze` ausführen, Fehler direkt fixen.
**Nach allen Tasks:** `flutter build apk --debug` um sicherzustellen dass alles kompiliert.
